version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd client
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: client/build
